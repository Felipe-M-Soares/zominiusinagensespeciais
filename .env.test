# Valores falsos, só pra permitir os testes automatizados rodarem —
# o cliente do Supabase (src/lib/supabase.ts) recusa iniciar sem essas
# duas variáveis definidas, mas os testes nunca fazem uma chamada de
# rede de verdade, então qualquer valor aqui serve.
VITE_SUPABASE_URL=https://test.supabase.co
VITE_SUPABASE_ANON_KEY=test-anon-key
